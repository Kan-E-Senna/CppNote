# 5. CLOSE-WAIT 和 TIME-WAIT 的状态和意义

CLOSE-WAIT是在第二次挥手服务端进入的状态，保留这个状态是因为服务端可能仍然有数据需要发送给客服端。

TIME-WAIT是第三次挥手，服务端发送了一个FIN包给客户端后客户端进入的状态。客户端是不可以在接收服务端的一个FIN报文之后立刻就进入CLOSED状态，原因有两点，第一，服务端发送的数据包有可能晚于这个FIN报文到达客户端，如果这个时候客户端凑巧使用了相同的端口，就会发生不可预知的异常。第二，客户端再第四次挥手时发送的ACK报文有可能没有及时到达服务端，这个时候是需要客户端进行超时重传的。

